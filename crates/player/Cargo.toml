[package]
name = "player"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
macroquad = { version = "0.4.14", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
