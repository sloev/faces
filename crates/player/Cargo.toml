[package]
name = "player"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
macroquad = "=0.4.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
