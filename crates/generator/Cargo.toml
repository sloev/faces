[package]
name = "generator"
version = "0.1.0"
edition = "2021"

[dependencies]
shared = { path = "../shared" }
ort = "2.0.0-rc.12"
delaunator = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
image = "0.24"
ndarray = "0.15"
